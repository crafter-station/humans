# Keep every Profile inside the protected directory

Humans will expose Profile data only to authenticated Members and authorized API or MCP clients, and will prevent external search indexing. Imported Profiles are searchable by default with suppression, correction, and claim controls; Member-controlled Profiles appear in search only after the Member opts in.
