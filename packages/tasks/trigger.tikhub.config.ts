import { defineConfig } from "@trigger.dev/sdk";

export default defineConfig({
  project: "proj_umusurvkybxuonbiopal",
  dirs: ["./src/trigger-tikhub"],
  maxDuration: 3600,
  runtime: "node-24",
});
